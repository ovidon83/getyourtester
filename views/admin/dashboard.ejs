<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | GetYourTester</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .admin-badge {
      font-size: 0.7rem;
      padding: 0.25rem 0.5rem;
      vertical-align: middle;
    }
  </style>
  <script>
    // Set a default value for activePage if not provided
    const activePage = '<%= typeof activePage !== "undefined" ? activePage : "dashboard" %>';
  </script>
</head>
<body>
  <!-- Single unified navigation bar -->
  <header class="header sticky-top shadow-sm">
    <nav class="navbar navbar-expand-lg bg-white">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <i class="fas fa-cube text-primary me-2"></i>
          <span class="fw-bold">GetYourTester</span>
          <span class="badge bg-primary ms-2 admin-badge">Admin</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? 'active' : '' %>" href="/admin/dashboard">Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'testers' ? 'active' : '' %>" href="/admin/testers">Testers</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'prs' ? 'active' : '' %>" href="/admin/prs">Test Requests</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'applications' ? 'active' : '' %>" href="/admin/applications">Applications</a>
            </li>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/about">About</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/contact">Contact</a>
            </li>
            <li class="nav-item">
              <a href="/auth/logout" class="nav-link">
                <i class="fas fa-sign-out-alt me-1"></i> Logout
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <main class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h3"><%= title %></h1>
    </div>

    <% 
      // Default to dashboard if activePage is undefined
      const currentPage = typeof activePage !== 'undefined' ? activePage : 'dashboard'; 
    %>

    <% if (currentPage === 'dashboard') { %>
      <!-- Dashboard Content -->
      <div class="row g-4">
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex justify-content-between align-items-center mb-3">
                <h2 class="h5 mb-0">Active Testers</h2>
                <div class="icon-circle bg-light">
                  <i class="fas fa-users text-primary"></i>
                </div>
              </div>
              <h3 class="display-4 mb-0">0</h3>
              <p class="text-muted mt-2 mb-0">Approved testers</p>
            </div>
            <div class="card-footer bg-transparent border-0">
              <a href="/admin/testers" class="btn btn-sm btn-outline-primary">View All</a>
            </div>
          </div>
        </div>
        
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex justify-content-between align-items-center mb-3">
                <h2 class="h5 mb-0">Pending Applications</h2>
                <div class="icon-circle bg-light">
                  <i class="fas fa-clipboard-list text-primary"></i>
                </div>
              </div>
              <h3 class="display-4 mb-0">0</h3>
              <p class="text-muted mt-2 mb-0">Awaiting review</p>
            </div>
            <div class="card-footer bg-transparent border-0">
              <a href="/admin/applications" class="btn btn-sm btn-outline-primary">Review</a>
            </div>
          </div>
        </div>
        
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex justify-content-between align-items-center mb-3">
                <h2 class="h5 mb-0">Active Tests</h2>
                <div class="icon-circle bg-light">
                  <i class="fas fa-code-branch text-primary"></i>
                </div>
              </div>
              <h3 class="display-4 mb-0">0</h3>
              <p class="text-muted mt-2 mb-0">Under testing</p>
            </div>
            <div class="card-footer bg-transparent border-0">
              <a href="/admin/prs" class="btn btn-sm btn-outline-primary">View All</a>
            </div>
          </div>
        </div>
      </div>
      
      <div class="row mt-4">
        <div class="col-12">
          <div class="card border-0 shadow-sm">
            <div class="card-header bg-transparent">
              <h2 class="h5 mb-0">Recent Activity</h2>
            </div>
            <div class="card-body">
              <p class="text-center text-muted my-5">No recent activity to display</p>
            </div>
          </div>
        </div>
      </div>
    <% } else if (currentPage === 'testers') { %>
      <!-- Testers Management Content -->
      <div class="row">
        <div class="col-12">
          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> Tester management functionality will be implemented soon.
              </div>
              <p class="text-center text-muted my-5">No testers have been added yet.</p>
            </div>
          </div>
        </div>
      </div>
    <% } else if (currentPage === 'prs') { %>
      <!-- Test Requests Content -->
      <div class="row">
        <div class="col-12">
          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> Test Request management functionality will be implemented soon.
              </div>
              <p class="text-center text-muted my-5">No test requests have been submitted yet.</p>
            </div>
          </div>
        </div>
      </div>
    <% } else if (currentPage === 'applications') { %>
      <!-- Applications Content -->
      <div class="row">
        <div class="col-12">
          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> Tester application review functionality will be implemented soon.
              </div>
              <p class="text-center text-muted my-5">No applications have been received yet.</p>
            </div>
          </div>
        </div>
      </div>
    <% } %>
  </main>

  <footer class="bg-light py-4 mt-5">
    <div class="container">
      <p class="text-center mb-0">&copy; <%= new Date().getFullYear() %> GetYourTester. All rights reserved.</p>
    </div>
  </footer>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 